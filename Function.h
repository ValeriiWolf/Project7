#pragma once

namespace MyFunc
{
	void PrintArrayFloat(float* arr, size_t size);
	float* InitArrayFloat(float* arr, size_t size);
	void CounterElementsArr(float* arr, size_t size);
}

void PrintArr2(int *arr, size_t size);
void bubbleSort(int arr[], int n);
